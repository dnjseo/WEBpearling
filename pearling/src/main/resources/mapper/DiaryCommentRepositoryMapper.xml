<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pearling.web.repository.DiaryCommentRepository">
      
   <select id="findAll" resultType="com.pearling.web.entity.DiaryComment">
      select * from diary_comment 
      where reg_member_id=#{memberId} and diary_post_id=#{diaryId}
   </select>
   
   <select id="countByDiary" resultType="int">
      select count(diary_id) from diary_comment 
      where diary_post_id=#{diaryId}
   </select>
   
   <insert id="save" parameterType="com.pearling.web.entity.DiaryComment">
      insert into diary_comment(reg_member_id, content, diary_post_id, reg_member_nickname) 
      values(#{regMemberId}, #{content}, #{diaryPostId}, #{regMemberNickname})
   </insert>
   
   <delete id="delete" parameterType="com.pearling.web.entity.DiaryComment">
	  delete from diary_comment
	  where member_id=#{memberId} and diary_post_id=#{diaryId}
   </delete>
   
</mapper>