<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pearling.web.repository.RoleRepository">
	<!-- 회원 이름으로 회원 정보와 역할 정보를 함께 조회 -->
	<select id="findById" resultType="Role">
		SELECT  r.*
		FROM member m
		INNER JOIN role r ON m.role_id = r.id
		WHERE m.role_id = #{id}
        limit 1;
	</select>
</mapper>
